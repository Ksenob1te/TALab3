integer dosmth(integer a, integer b)
start
    integer c := a + b;
    integer getd()
    start
        return c + a;
    finish;
    return call getd;
finish;

integer main()
start
    mutable integer a;
    mutable integer a := 10;
    integer a := 10;

    a := 10 * (2 + 1);
    integer b := a * 15;
    integer res := call dosmth with a, b;
    string c := "hello" + " world";
    string d := "bye world";

    pointer ptrc := &c;
    pointer ptrd := &d;
    pointer ptrdosmth := &dosmth;
    integer resd := call *ptrdosmth with a, b;

    array of pointer abc [0];
    integer counter := 0;
    while (counter < 10) start
        checkzero (counter % 2)
            right;
        instead
            abc.append(ptrd);
        counter := counter + 1;
    finish;

    c := "STOP";
    return 0;
finish;

